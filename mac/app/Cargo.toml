[package]
name = "orbit-app"
version = "0.3.3"
edition = "2021"
description = "Orbit for macOS"
license = "MIT"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
orbit-core = { path = "../core" }
serde_json = "1"
tauri = { version = "2", features = [] }
