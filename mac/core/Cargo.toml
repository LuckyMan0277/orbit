[package]
name = "orbit-core"
version = "0.3.3"
edition = "2021"
description = "Orbit host logic shared by the desktop shells: files, terminals, saved conversations"
license = "MIT"

[dependencies]
serde_json = "1"
portable-pty = "0.9"
encoding_rs = "0.8"
sha2 = "0.10"
base64 = "0.22"
